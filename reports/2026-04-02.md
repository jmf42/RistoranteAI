# Daily Call Review — 2026-04-02
Period reviewed: 2026-04-01 (yesterday)

---

## 1. KPI Summary

| Metric | Value | Baseline | Status |
|---|---|---|---|
| Total calls | 2 | — | — |
| Booking conversion rate | 0.0% | >50% | ⚠️ Well below baseline |
| ElevenLabs failure rate | 100.0% | <30% | ⚠️ Well above baseline |
| Avg duration | 24s | — | Very short |
| Escalations | 0 | — | — |

**Note:** 0% conversion and 100% ElevenLabs `call_successful: failure` on 2 calls. Sample is very small (2 calls), but both failing is a signal worth investigating, especially the party-size recognition miss in the transcript.

---

## 2. Failure Analysis

### Call 1 — 2026-04-01 06:16 UTC — 43s — info_provided

**What went wrong:** The user stated "una prenotazione di un tavolo per quattro" clearly in the opening message. The agent responded with the standard three-question prompt ("Per che giorno, a che ora e per quante persone?"), which is correct. The user then replied "Per quattro, per" (providing party size again but getting cut off before day/time). The agent then asked "non ho capito per quante persone. Può ripetere?" — failing to capture party size even though it had been stated twice. Call ended without a booking.

**Root cause:** Two compounding issues:
1. **Prompt compliance failure**: The prompt rule "Se il chiamante fornisce più dati insieme, non richiederli separatamente" and Operating Priority 4 ("Non ripetere informazioni già capite") were violated — the agent re-asked for party size that was clearly stated in the first user turn.
2. **Likely STT partial transcription**: The user's second turn "Per quattro, per" ends mid-sentence, suggesting they were cut off or the STT captured only a fragment. The agent may not have matched "per quattro" as a number due to the dangling "per."

**Severity:** High — a clear and motivated caller with a complete opening statement (party size stated) was lost because the agent failed to retain the already-stated information.

### Call 2 — no transcript available

**What went wrong:** No transcript data returned by the review script. Duration contributed to the 24s average (with Call 1 at 43s, Call 2 was likely ~5s or a near-immediate drop).

**Root cause:** Likely abandoned call / immediate hang-up before any meaningful exchange.

**Severity:** Low — expected behavior for very short calls.

---

## 3. Pattern Recognition

- **Party size extraction failure**: The single substantive transcript shows the agent failing to extract "quattro" from the first user message. The user stated the party size in their opening line ("un tavolo per quattro"), yet the agent asked for all three fields from scratch, then failed to pick it up in the second turn as well. This is a known risk when the initial prompt asks for all three fields simultaneously — the agent may not back-fill already-provided data.
- **Very short calls**: Both calls were short (43s and ~5s), indicating either poor connectivity, user drop-off, or agent-side confusion that caused the user to hang up.
- **No tool calls in transcripts**: Neither call progressed far enough to invoke any tool. The failure is upstream of tool execution.
- **100% ElevenLabs `call_successful: failure`**: Both calls were marked failed by ElevenLabs evaluation criteria (`reservation_completed: failure`). This is accurate for Call 1 (no reservation completed) and consistent with a hang-up for Call 2. Not a detection bug — the evaluation is correct.

---

## 4. Prompt Recommendations

**Recommendation 1 — Explicitly instruct the agent to extract party size from the user's opening message before asking the three-question prompt.**

```
SECTION: New Booking — step 1
CURRENT:
"Per che giorno, a che ora e per quante persone?"

PROPOSED:
Before asking the three-question prompt, check if the caller already stated party size,
day, or time in their opening message. If any of those values were already given, ask
only for the missing ones. If only day and time are missing, ask:
"Per che giorno e a che ora?"

WHY: In the 2026-04-01 06:16 call, the user stated "per quattro" in their first sentence,
but the agent asked for all three fields including party size again, then failed to
recognize the repeated answer "per quattro."
```

**Recommendation 2 — Add a note on partial user responses with trailing "per".**

```
SECTION: Audio Handling
CURRENT:
"Se non capisci, chiedi solo la parte mancante."

PROPOSED:
Add example: if the user says "per quattro, per..." and is cut off, treat "quattro"
as the confirmed party size and ask only for what is missing ("Per che giorno e a che ora?").

WHY: The transcript shows the user was cut off mid-sentence after stating party size;
asking "non ho capito per quante persone" when the agent received "per quattro, per"
lost the call.
```

---

## 5. Code Recommendations

No tool errors were observed in the transcripts. Both calls ended before any tool was called. No code changes recommended based on today's data.

---

## 6. ElevenLabs Config Recommendations

**Turn eagerness / interruption handling:** The user was cut off mid-sentence ("Per quattro, per") — the transcript ends abruptly. It is possible the agent's turn eagerness caused the agent to start speaking before the user finished. Consider verifying that eagerness is set to "Normal" (not high/aggressive) in ElevenLabs conversation flow settings. This is already documented in the config notes but worth re-confirming against the live agent config.

No other config changes recommended based on 2 calls with minimal transcript data.

---

## 7. No-Action Items

- **Call 2 (~5s, no transcript):** A near-immediate hang-up before the agent could respond is expected behavior (wrong number, accidental dial, etc.). No changes needed.
- **ElevenLabs marking both calls as `failure`:** This is accurate — no reservation was completed. The evaluation criteria are working correctly. Not a false positive.

---

## 8. Yesterday vs Baseline

| Metric | Yesterday | Baseline | Delta |
|---|---|---|---|
| Conversion rate | 0.0% | >50% | -50pp |
| ElevenLabs failure rate | 100.0% | <30% | +70pp |

Both metrics are far from baseline, but the sample size is only 2 calls on an early-morning weekday window (both before 07:00 UTC). The 06:16 UTC call is the only one with a real interaction, and the root cause is identifiable (party-size extraction miss + likely STT cut-off). No systemic tool or infrastructure failure is evident. Revisit after a larger daily sample.
